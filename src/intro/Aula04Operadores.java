package intro;

public class Aula04Operadores {
    public static void main(String[] args) {
        // operadores logicos: && (AND) || (OR) ! (NOT)
        // OPERADORES DE ATRIBUIÇÃO: = -= += *= /= %= ++ __
    }
}
